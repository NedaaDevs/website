# Build-time only. Used by scripts/fetch-stats.ts to authenticate against
# the Nedaa API for the Operational Ledger snapshot. Not exposed to the
# client bundle. In production, requests from the deployed origin should
# be allowed without a token; this is mainly for local dev.
NEDAA_ADMIN_KEY=
